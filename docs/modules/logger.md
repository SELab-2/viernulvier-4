# Logger Module

**Category:** Shared/Utility

## Overview

This module provides the central `AppLogger` service for the backend. It ensures consistent log formatting and output across all feature modules.

## Module Boundaries

### Dependencies
No external dependencies.

### Exports
* `AppLogger`